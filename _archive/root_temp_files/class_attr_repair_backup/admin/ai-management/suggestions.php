<?php
$page_title = $page_title ?? 'AI Property Suggestions';
$suggestions = $suggestions ?? [];
?>
<div class="d-flex justify-content-between align-items-center mb-4">
    <div>
        <h1 class="h3 mb-0">AI Property Suggestions</h1>
        <p class="text-muted mb-0">Personalized property recommendations generated by AI</p>
    </div>
</div>

<div class="card border-0 shadow-sm">
    <div class="card-header bg-white py-3">
        <h5 class="mb-0">Suggestions</h5>
    </div>
    <div class="card-body p-0">
        <div class="table-responsive">
            <table class="table table-hover mb-0">
                <thead>
                    <tr>
                        <th>User</th>
                        <th>Property</th>
                        <th>Score</th>
                        <th>Reason</th>
                        <th>Algorithm</th>
                        <th>Date</th>
                    </tr>
                </thead>
                <tbody>
                    <?php if (empty($suggestions)): ?>
                        <tr><td colspan="6" class="text-center py-4 text-muted">No suggestions yet</td></tr>
                    <?php else: ?>
                        <?php foreach ($suggestions as $row): ?>
                            <tr>
                                <td>
                                    <strong><?= htmlspecialchars($row['user_name'] ?? 'Guest') ?></strong>
                                    <br><small class="text-muted"><?= htmlspecialchars($row['user_email'] ?? '') ?></small>
                                </td>
                                <td><?= htmlspecialchars(mb_substr($row['property_title'] ?? 'Property #'.$row['property_id'], 0, 40)) ?></td>
                                <td class="style-36825">
                                    <div class="d-flex align-items-center gap-2">
                                        <div class="progress flex-grow-1 style-87912">
                                            <div class="progress-bar bg-<?= ($row['score'] ?? 0) >= 80 ? 'success' : (($row['score'] ?? 0) >= 50 ? 'warning' : 'danger') ?>" class="style-83425"></div>
                                        </div>
                                        <small class="fw-bold"><?= (int)($row['score'] ?? 0) ?>%</small>
                                    </div>
                                </td>
                                <td><small class="text-muted"><?= htmlspecialchars(mb_substr($row['reason'] ?? '', 0, 80)) ?></small></td>
                                <td><span class="badge bg-secondary">v<?= htmlspecialchars($row['algorithm_version'] ?? '1.0') ?></span></td>
                                <td><?= date('M j, Y', strtotime($row['created_at'] ?? 'now')) ?></td>
                            </tr>
                        <?php endforeach; ?>
                    <?php endif; ?>
                </tbody>
            </table>
        </div>
    </div>
</div>
