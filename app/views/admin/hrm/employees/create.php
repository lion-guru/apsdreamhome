<?php
$page_title = $page_title ?? 'Create Employee - APS Dream Home';
$page_heading = $page_heading ?? 'Create Employee';

?>
<div class="container-fluid py-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="mb-0"><i class="fas fa-cog me-2"></i><?= htmlspecialchars($page_heading ?? __('hr_add_employee')) ?></h2>
    </div>
    <div class="card border-0 shadow-sm">
        <div class="card-body text-center py-5">
            <i class="fas fa-construction fa-4x text-muted mb-3"></i>
            <h4 class="text-muted"><?= __('hr_add_employee') ?></h4>
            <p class="text-muted">This module is under development. Check back soon.</p>
        </div>
    </div>
</div>
?>
<?php
$page_title = $page_title ?? 'Designations - APS Dream Home';
$page_heading = $page_heading ?? 'Designations';

?>
<div class="container-fluid py-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="mb-0"><i class="fas fa-cog me-2"></i><?= htmlspecialchars($page_heading ?? 'Designations') ?></h2>
    </div>
    <div class="card border-0 shadow-sm">
        <div class="card-body text-center py-5">
            <i class="fas fa-construction fa-4x text-muted mb-3"></i>
            <h4 class="text-muted">Designations</h4>
            <p class="text-muted">This module is under development. Check back soon.</p>
        </div>
    </div>
</div>
?>
<?php
$page_title = $page_title ?? 'Departments - APS Dream Home';
$page_heading = $page_heading ?? 'Departments';

?>
<div class="container-fluid py-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="mb-0"><i class="fas fa-cog me-2"></i><?= htmlspecialchars($page_heading ?? 'Departments') ?></h2>
    </div>
    <div class="card border-0 shadow-sm">
        <div class="card-body text-center py-5">
            <i class="fas fa-construction fa-4x text-muted mb-3"></i>
            <h4 class="text-muted">Departments</h4>
            <p class="text-muted">This module is under development. Check back soon.</p>
        </div>
    </div>
</div>
?>
<?php
$page_title = $page_title ?? 'Attendance - APS Dream Home';
$page_heading = $page_heading ?? 'Attendance';

?>
<div class="container-fluid py-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="mb-0"><i class="fas fa-cog me-2"></i><?= htmlspecialchars($page_heading ?? 'Attendance') ?></h2>
    </div>
    <div class="card border-0 shadow-sm">
        <div class="card-body text-center py-5">
            <i class="fas fa-construction fa-4x text-muted mb-3"></i>
            <h4 class="text-muted">Attendance</h4>
            <p class="text-muted">This module is under development. Check back soon.</p>
        </div>
    </div>
</div>
<?php

?>