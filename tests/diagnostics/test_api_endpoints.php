<?php
// API endpoints test
$endpoints = [
    "/api/health",
    "/api/properties", 
    "/api/leads"
];

foreach ($endpoints as $endpoint) {
    echo "Testing: $endpoint\n";
    // Test endpoint logic here
}
?>